import { StyleSheet,StatusBar } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        flex: 1,
      },
    bienvenida:{
      fontSize:25
    },
    bienvenidaContainer:{
      padding:30,
      alignItems:'center'
    }
})